# bpftrace Tools Corpus

These `.bt` files are copied from the [upstream bpftrace tools directory](https://github.com/bpftrace/bpftrace/tree/master/tools) and used as a real-world formatter acceptance corpus.

Only current tool scripts are retained. Upstream build files and historical `old/` variants are intentionally excluded.
